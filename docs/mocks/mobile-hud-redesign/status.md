feature: mobile-hud-redesign
status: approved
approved-version: mock-v2.html
feedback:
- v1: three parallel directions (A edge rails / B left rail + thumb dock / C corner pods). User picked a mix rather than one: "Option A - the bottom two rows" plus board moved up, one equal-spaced bottom row carrying C's resource cards on the left (layered leftmost-on-top so every count stays visible) and B's card stack on the right.
- v2: board margin iterated flush -> 5% -> 3%; settled on 3% (11px a side, hexes 366x338 = 37.6% of a 390x844 phone). Roll and End turn merged into a single right-hand button that flips label on the player's own input only. Approved.
